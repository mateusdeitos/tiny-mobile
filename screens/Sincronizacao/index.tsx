import React from "react";
import { View, Text } from 'react-native';
import { styles } from './styles';

export const Sincronizacao: React.FC = ({ children }) => {
	return (
		<View style={styles.container}>
			<Text>Contatos</Text>
		</View>
	)
}